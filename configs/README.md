Examples of config files for the reviewer and AI agents.

When using the Codex agent command from `reviewer.config.yaml`, install
`codex.config.toml` as `/var/lib/upsource-ai-reviewer/codex/config.toml`.
